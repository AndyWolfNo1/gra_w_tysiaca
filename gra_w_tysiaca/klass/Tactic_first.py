from gra_w_tysiaca.klass.Tactics import *

class Tactic_first(Tactic):
    
    def take_musik(self):
        pass
        #alls = []
        #self.sum_points()
        #for i in range(4):
            #color = self.cards[0].colors[i]
            #print(self.master[color])
            #print(self.player_name, 'Tu1:', pkt_master)
            #pkt_master_p = self.master_p[color]
            #print(self.master_t[color])
            #suma = pkt_master + pkt_master_p
            #alls.append(suma)
        #return alls
            #pkt_c += self.point_t[self.cards[0].colors[i]]
            #suma = pkt_master + pkt_c
        #return suma
            
                    
